services:
  - type: worker
    name: spellbook-tg-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: SITE_URL
        value: https://0654c680.neon-nft.pages.dev
